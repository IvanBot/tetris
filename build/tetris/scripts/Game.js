//Игровая механика
/*window.onload = function() {
	var start_state = new XMLHttpRequest();
	start_state.open('GET', 'scripts/start_state.json', false);
	start_state.send(null);

	var start_state = JSON.parse(start_state.responseText);

	for (i = 0; i< start_state.length; i++)
	{
		start_state[i]
	}
}*/